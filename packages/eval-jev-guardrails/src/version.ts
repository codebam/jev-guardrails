/** Version of `@codebam/eval-jev-guardrails`; kept in sync with package.json. */
export const PACKAGE_VERSION = '0.1.0'

/** First `@codebam/dsh-jev-guardrails` release that ships its own dsh bundle patch. */
export const DSH_GUARDRAILS_RANGE = '^0.1.1'

/** Default hosted service base URL. */
export const DEFAULT_BASE_URL = 'https://eval.seanbehan.ca'
